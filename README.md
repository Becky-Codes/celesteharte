Version 1.2 change log:

- Added mobile view!
  
- Adjusted text in the About Me section to rescale based upon the size of the window. Changed the contents of the flex box to be a column instead of a row
- Adjusted all of the flex box containers to switch to column mode when the window becomes smaller than 1000px
- Changed up the pop-up to change sizes when the screen gets below 1000px
- Changed the font of the pop-up art
- Changed the size of the photo in the pop up art when the screen size gets below 1000px
